THIS SOURCE IS PROMOTED EPISODIC MEMORY. Its facts already live in the episodic store, recurred across sessions, and the user explicitly chose to turn them into a note (`/promote`). The usual routing is therefore INVERTED here:

- The personal, time-bound facts ARE the note body — copy them into it verbatim. Do NOT withhold a fact from the body because it is personal or time-bound; that is exactly the material being promoted.
- `"ephemerals"` MUST be an empty array (`[]`). Re-emitting these facts there would duplicate what they were promoted out of. The key/date rules below apply only when there is something to emit — here there never is.
